# Workspace Behavioral Rules

## Git Commit Protocol
- **NEVER perform git commits or git pushes automatically** unless the user explicitly requests it.
- Always wait for explicit user approval/instruction before creating git commits or pushing to remote repositories.
